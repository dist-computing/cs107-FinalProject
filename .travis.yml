language: python
python:
    - "3.6"
before_install:
    - pip install -r requirements.txt
    - pip install pytest pytest-cov
    - pip install codecov
script:
    - cp -r AAD tests/
    - cd tests
    - pytest --cov=./
after_success:
    - codecov
